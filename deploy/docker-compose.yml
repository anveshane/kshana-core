services:
  kshana-core-prod:
    image: ${PROD_IMAGE:-kshana-core:latest}
    container_name: kshana-core-prod
    restart: unless-stopped
    env_file:
      - .env.prod
    environment:
      PORT: 8080
      NODE_ENV: production
    expose:
      - "8080"

  kshana-core-dev:
    image: ${DEV_IMAGE:-kshana-core:latest}
    container_name: kshana-core-dev
    restart: unless-stopped
    env_file:
      - .env.dev
    environment:
      PORT: 8080
      NODE_ENV: production
    expose:
      - "8080"

  nginx:
    image: nginx:1.27-alpine
    container_name: kshana-core-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "81:81"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
